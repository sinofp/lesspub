{
  "name": "lesspub",
  "version": "0.1.0",
  "scripts": {
    "res:build": "rescript",
    "res:clean": "rescript clean",
    "res:dev": "rescript watch",
    "res:fmt": "rescript format",
    "build": "npm run res:build && esbuild src/Send.js --bundle --platform=node --external:*.json --outfile=dist/Send.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "author": "sinofp",
  "license": "BSD-2-Clause",
  "dependencies": {
    "rescript": "^12.2.0"
  }
}

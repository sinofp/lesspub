{
  "name": "lesspub",
  "version": "0.1.0",
  "scripts": {
    "res:build": "rescript",
    "res:clean": "rescript clean",
    "res:dev": "rescript build -w",
    "res:fmt": "rescript format -all",
    "build:bundle": "npm run res:build && esbuild src/Send.js --bundle --platform=node --outfile=dist/Send.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "author": "sinofp",
  "license": "BSD-2-Clause",
  "dependencies": {
    "rescript": "^10.1.2"
  }
}
